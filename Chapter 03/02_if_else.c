#include <stdio.h>

int main(){
    int age = 5;

    if (age > 10)
    {
        printf("we are inside if");
        printf("\nyour age is greater than 10");
    
    }
    else {
        printf("your age is not greater than 10");
    }
    return 0;
}