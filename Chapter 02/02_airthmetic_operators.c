#include <stdio.h>

int main()
{
    int a = 6;
    int b = 5;
    int c = a + b;
    printf("the value of a is %d and the value of b is %d and the sum of the value of a and b is %d", a, b, c);

    // the modulus operator is used to find the remainder.
    
    printf("\nthe remainder when a is divided by b is %d", a % b);

    return 0;
    }